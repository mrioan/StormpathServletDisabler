# StormpathServletDisabler

## Configuration

Stormpath is disabled by default. If you want it to be enabled then an environment variable like this must be specified:

```
export APP_EDITION=COMMERCIAL
```

## Run

```
mvn tomcat7:run
```

